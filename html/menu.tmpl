<!DOCTYPE html>
<html lang="ja">
<head>
    <title>Cafe LeSinaのメニュー | 焼き物の町、益子のカフェ</title>
    <meta name="description" content="焼き物/陶器で有名な町、益子にあるカフェLeSiNaのメニュー">
    {{> head }}
</head>
<body>
    <div class="ui vertical masthead center aligned segment site-cover">
        {{> header}}
        {{> nav }}

        <div class="ui text container">
            <div class="menu-list">
                <ol class="ui breadcrumb" itemscope itemtype="http://schema.org/BreadcrumbList">
                    {{#breadcrumb.menu}}
                        <li>
                            {{#active}}
                                <a itemprop="item" href="{{url}}" class="section active"><span itemprop="name">{{name}}</span></a>
                            {{/active}}
                            {{^active}}
                                <a itemprop="item" href="{{url}}" class="section"><span itemprop="name">{{name}}</span></a>
                            {{/active}}
                            <meta itemprop="position" content="{{@index + 1}}" />

                            {{^active}}
                            <i class="right angle icon divider"></i>
                            {{/active}}
                        </li>
                    {{/breadcrumb.menu}}
                </ol>

                <h1 class="page-title"><i class="food icon"></i>メニュー表</h1>

                <ul class="ui list">
                    {{#menu.names}}
                    <li>{{name}} {{#set}}※{{/set}} ... {{price}}円</li>
                    {{/menu.names}}
                </ul>

                <p class="note">
                    ※付きメニューは、サラダ、コーヒーが付いています。
                </p>

                <div class="gallery" id="photos">
                    {{#menu.images}}
                    <a href="{{src}}">
                        <img src="{{src_thumbnail}}">
                    </a>
                    {{/menu.images}}
                </div>
            </div>
        </div>
    </div>

    
</body>
